<!-- header -->
<header class="header" id="navbar">
    <nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top l-navbar">
        <div class="container">
            <a class="navbar-brand my-0" href="<%= relPath %>"><%= config.commons.sitename %></a>
            <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarList" aria-controls="navbarList" aria-expanded="false" aria-label="ナビゲーションの切替">
                <span class="navbar-toggler-icon"></span>
<%
if(plugins.svghexagon) {
-%>
<%- include('../_plugins/svghexagon/svghexagon', { commonVar }) %>
<%
}
-%>
            </button>
<% const navPath = funcNavPath(relPath, id) -%>
            <div class="collapse navbar-collapse" id="navbarList">
                <ul class="nav navbar-nav ml-auto">
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" id="dropdownFeature" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">機能</a>
                        <div class="dropdown-menu" aria-labelledby="dropdownFeature">
                            <a class="dropdown-item" href="<%= navPath %>#features">特徴</a>
                            <a class="dropdown-item" href="<%= navPath %>wordpress.html">WordPress連携(β版)</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="<%= navPath %>#how2use">使い方</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="<%= navPath %>#origin">由来</a>
                    </li>
<%
if(plugins.news) {
-%>
                    <li class="nav-item">
                        <a class="nav-link" href="<%= navPath %>#<%= config.param.news.baseurl %>"><%= config.param.news.title %></a>
                    </li>
<%
}
-%>
<%
if(plugins.sitemap) {
-%>
                    <li class="nav-item">
                        <a class="nav-link" href="<%= navPath %>sitemap.html">サイトマップ</a>
                    </li>
<%
}
-%>
<%
if(plugins.sitesearch) {
-%>
                    <li class="nav-item">
                        <a class="nav-link" href="<%= navPath %>sitesearch.html">サイト内検索</a>
                    </li>
<%
}
-%>
                </ul>
            </div>
        </div>
    </nav>
</header>
<!-- /header -->